/**
 * @brief Checks if the user has permission to perform a certain action.
 * Only specific roles are allowed.
 *
 * @param user The user object.
 * @return True if the user is allowed, otherwise false.
 */
bool isUserAllowed(User user) {
    // Implementation code here
}
